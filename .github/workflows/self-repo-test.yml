name: Self-Repo Syntax Test v2
on: workflow_dispatch

jobs:
  test-self-repo:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Use self-repo action (path form)
        uses: $/.github/actions/self-test
      - name: Also test ./ form
        uses: ./.github/actions/self-test
